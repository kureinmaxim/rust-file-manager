# Build artifacts
/target

# Secrets — never commit
.env

# User data
/uploads/
/users.json

# Local-only files (private configs, legacy code, personal data)
/_local/

# OS / IDE noise
.DS_Store
.idea/
.vscode/
*.log
debug_log.txt
